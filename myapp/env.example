# API Configuration
# Backend API base URL
# For production, use your deployed backend URL
EXPO_PUBLIC_API_BASE=http://localhost:4000/api

# Socket.io Configuration (Optional - for future real-time features)
# Socket.io server URL
EXPO_PUBLIC_SOCKET_IO_URL=http://localhost:4000
EXPO_PUBLIC_SOCKET_IO_PATH=/socket.io

# Offline Sync Configuration
# Maximum number of location updates to queue when offline
EXPO_PUBLIC_OFFLINE_QUEUE_LIMIT=100
# Retry interval in milliseconds for failed sync attempts
EXPO_PUBLIC_SYNC_RETRY_INTERVAL=5000
# Maximum retry attempts before giving up
EXPO_PUBLIC_SYNC_MAX_RETRIES=3

# Note: In Expo, environment variables must be prefixed with EXPO_PUBLIC_ to be accessible in the app
# After updating this file, rename it to .env and update the values for your environment
