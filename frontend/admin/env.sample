# API Configuration
# Backend API base URL
VITE_API_BASE=http://localhost:4000/api

# Socket.io Configuration
# Socket.io server URL (should match backend SOCKET_IO_PATH)
# For production, use your deployed backend URL
VITE_SOCKET_IO_URL=http://localhost:4000
VITE_SOCKET_IO_PATH=/socket.io

# Feature Flags
# Enable real-time updates via Socket.io (true/false)
# Falls back to polling if disabled or connection fails
VITE_ENABLE_REALTIME=true




